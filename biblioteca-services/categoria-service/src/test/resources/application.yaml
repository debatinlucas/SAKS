spring:
    application:
        name: categoria-service
    datasource:
        url: jdbc:mysql://localhost:3306/biblioteca_teste
        username: root
        password: master
        tomcat:
            testOnBorrow: true
            validationQuery: SELECT 1
        sqlScriptEncoding: UTF-8
    jpa:
        hibernate:
            ddlAuto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        showSql: true

server:
  port: 6065
